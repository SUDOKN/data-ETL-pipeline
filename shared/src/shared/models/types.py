LLMMappingType = dict[str, list[str]]  # mapping from known concept to unknown concepts
MfgURLType = str
OntologyVersionIDType = (
    str  # version of the ontology, used for receipt log and consistency
)
