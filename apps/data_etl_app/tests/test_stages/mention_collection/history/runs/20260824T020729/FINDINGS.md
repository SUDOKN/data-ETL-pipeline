# Findings — run `20260824T020729` (the instrument's first pass)

**This is a baseline pass, not an evaluation of a current run.** The mechanical
half ran clean over the whole run; nothing has been judged, because a judged
census needs a run on the current pipeline code and none exists yet.

`SUMMARY.md` is regenerated by every mechanical pass — analysis lives here.

## Why this run

`20260824T020729` is the **byte-exact mention stage of the completed
end-to-end run `20260825T194457`**: all 14 (subject, field) pairs match on
prompt version, group count and mention total, because `194457` replayed its
mention answers from this run. It is also the newest run that carries a `fold`
block at all — full-run dumps only began writing one on 2026-08-27.

So it is the right baseline, and it is the **last** run of the old fold.

## Result

**14 of 14 fields OK. No tripwire fired.** 4,828 judgeable items enumerated
into work orders and awaiting readers.

| | |
|---|---|
| fields with mention data | 14 / 14 |
| tripwire failures | 0 |
| mentions | 6,085 |
| distinct snippets (global / windowed) | 3,345 / 4,164 |
| groups | 2,248 |
| judgeable items enumerated | 4,828 |
| judged | 0 |

Every fixed defect this instrument has a tripwire for was verified absent over
real data: 6,085 mentions each carrying their own form whole-word in their own
snippet; no duplicate occurrence at any span; no mention-id collision; no URL,
banned opener, verb opener or cross-reference in any location; no mention on a
dropped legal page; `products` byte-identical to `contract_products` on both
subjects.

That is a genuinely clean bill for the mechanical half — and it is what a
deterministic stage *should* look like, which is why these gates are exact
rather than banded.

## The one shape finding

**`steelcraft_com__products` already carries a group of 60 distinct entries**,
against synthesis's packing limit of 50. One group, pre-D8. This is the metric
handed to the synthesis eval (`M7`, `BOUNDARY.md`), and it is the number to
watch hardest on the next run: the D8 reversal is expected to push far more
groups past that line, because a generic head noun now inherits every specific
occurrence containing it.

## What this pass could NOT see

- **Nesting is 0 by construction.** Longest-span containment was still
  suppressing nested hits, so `nested_occurrences` is zero everywhere — the
  correct answer, and the reason the detector is pinned by its own test rather
  than trusted because it reported a zero.
- **Nothing judged.** Location correctness (S2∧S3∧S4), snippet extent (S1),
  grouping (G1–G4) and inheritance (I1) are all unmeasured. The location layer
  in particular has never been measured by anyone on this project.
- **Golden checks reported but gated nothing** — all 14,713 labels are
  `candidate` until the second verification pass.

## What to expect on the first run of the current code

The stage changed on 2026-08-27 (`bbfc42b`, then `482b0c0`). Predictions, so the
next pass is a test rather than a description:

1. **Mentions rise by roughly a third.** Containment was measured to hide 34% of
   all occurrences.
2. **Empty groups collapse toward zero**, and whatever remains is a *search*
   false positive — the fold's own docstring now says so. Hand the residue to
   the search eval; do not gate on it here.
3. **`nested_occurrences` becomes non-zero for the first time**, creating the
   I1 population. Nothing is known about its dilution rate.
4. **`groups_over_50_entries` grows** — the dump's own comment records `door`
   going 60 → 157 entries.
5. **A `collapsed` status appears** (D21). This instrument knows it; the
   grounding harness does not, and will emit spurious `status_accounting`
   findings until told (`BOUNDARY.md`).
6. `doc_span` is now on every mention, so golden labels anchored by offset can
   be checked directly rather than only by quote.

If (1) and (3) do **not** happen, the run did not pick up the new code — check
`fold.collapse_compounds` in the dump header before reading anything else.

## Protocol note

Run the mechanical pass with `checks/run_eval.py --run <id>`, then follow
`RUNBOOK.md` from step 2. Do not compare any number here to a post-2026-08-27
run without saying which side of the change it came from: they measure
different quantities, and the `config_digest` on each scorecard is what tells
them apart.
