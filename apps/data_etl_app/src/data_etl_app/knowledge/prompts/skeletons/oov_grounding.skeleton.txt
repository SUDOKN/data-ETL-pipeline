You will be given a list of records, each carrying a masked id. Each record gathers the evidence about one recurring subject in text scraped from a manufacturer's website: a "focal_form" — the wording the site itself uses for that subject — and a "synthesis" of what the text says about it and what the manufacturer does with or in relation to it. Each record also lists what has already been identified from it, possibly nothing — each already-identified result naming one {{entity_noun}}.

You will also be given the vocabulary that earlier identification matched against, written as an indented outline. Each line names one {{entity_noun}}, and a line indented under another is a narrower kind of the one above it. Where a line ends in "(also: …)", those are other names for that same {{entity_noun}}.

You will assess each record independently.

### Your job:
From each record's focal form and synthesis, identify every distinct {{entity_noun}} the record evidences that neither the provided options nor the record's already-identified results cover. You are looking for what the vocabulary misses. Name each one yourself, plainly and generically, the way the outline would name it if it carried it.

Identification is your whole job here; relationships are not. Whether it is the manufacturer itself or some other party that {{entity_relationships.third_person}} an identified {{entity_noun}} plays no part. Do not withhold a candidate because that relationship looks doubtful, and do not record one on the strength of that relationship alone.

### When nothing remains
It is normal to find nothing. For most records the vocabulary already covers everything the record evidences, and an empty "candidates" array is the expected result then. Hold every candidate you return to the same bar the rules state. Do not lower the bar to return something.

### Rules:
{{rules_block}}

{{report_block}}

### Output:
Return a JSON object with a single key "groundings" whose value is an array of objects, one per record, each carrying the record's id copied exactly as given. Where you identified nothing new, return an empty "candidates" array with a short "explanation" of why nothing remains for the record.

The example below shows a record yielding one new {{entity_noun}}, a record yielding two, and a record yielding none. Every candidate you return carries its own complete set of applied rules. Everything in angle brackets is yours to fill in.

```json
{{output_example}}
```

Each record must appear exactly once.
