You will be given a list of records, each carrying a masked id. Each record gathers the evidence about one recurring subject in text scraped from a manufacturer's website: "mentions" — what the text says about it and where, quoting the text verbatim — and a "synthesis" of what the manufacturer does with or in relation to it. Each of these records has already been established to evidence {{parent_entity}} as one {{entity_noun}}.

You will also be given a list of options. Each option represents a specific type of {{parent_entity}}. Each option also comes with its definition and example sub types for your reference.

You will assess each record independently.

### Your job:
From each record's mentions and synthesis, identify every distinct specific type of {{parent_entity}} the record evidences. Then, for each identified and qualifying type, choose a matching option from the options provided.

Identification is your whole job here; relationships are not. Whether it is the manufacturer itself or some other party that {{entity_relationships.third_person}} an identified type plays no part. What you owe each specific type is evidence: the record must evidence that specific type on its own merits, per the rules below.

### Rules:
{{rules_block}}

Types of {{parent_entity}} to choose from:
{{types_of_parent_entity}}

{{report_block}}

### Output:
Return a JSON object with a single key "groundings" whose value is an array of objects, one per record, each carrying the record's id copied exactly as given. Copy each chosen option name verbatim from its "name" field — do not paraphrase, re-case, or re-punctuate it. An option may also be one you proposed yourself as a sibling type. Where nothing more specific than {{parent_entity}} can be identified from the record, return an empty "options" array with a short "explanation" saying so.

The example below shows a record yielding one option, a record yielding several, and a record from which nothing more specific could be identified. Every option you return carries its own complete set of applied rules. Everything in angle brackets is yours to fill in.

```json
{{output_example}}
```

Each record must appear exactly once.
