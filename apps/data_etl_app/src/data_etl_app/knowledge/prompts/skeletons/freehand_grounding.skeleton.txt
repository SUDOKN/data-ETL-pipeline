You will be given a list of records, each carrying a masked id. Each record gathers the evidence about one recurring subject in text scraped from a manufacturer's website: a "focal_form" — the wording the site itself uses for that subject — and a "synthesis" of what the text says about it and what the manufacturer does with or in relation to it.

You will assess each record independently.

### Your job:
From each record's focal form and synthesis, identify every distinct {{entity_noun}} the record evidences, and name it yourself. What you return is always the category, never a specific make, model, or individual item.

Identification is your whole job here; relationships are not. Whether it is the manufacturer itself or some other party that {{entity_relationships.third_person}} an identified {{entity_noun}} plays no part. Do not withhold a candidate because that relationship looks doubtful, and do not record one on the strength of that relationship alone.

A record may also evidence no {{entity_noun}} at all, and recording none is the correct result then.

### Rules:
{{rules_block}}

{{report_block}}

### Output:
Return a JSON object with a single key "groundings" whose value is an array of objects, one per record, each carrying the record's id copied exactly as given. Where you identified nothing, return an empty "candidates" array with a short "explanation" of why the record yields no {{entity_noun}}.

The example below shows a record yielding one {{entity_noun}}, a record yielding several, and a record yielding none. Every {{entity_noun}} you return carries its own complete set of applied rules. Everything in angle brackets is yours to fill in.

```json
{{output_example}}
```

Each record must appear exactly once.
