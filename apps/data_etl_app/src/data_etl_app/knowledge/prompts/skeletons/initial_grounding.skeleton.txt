You will be given a list of phrases extracted from text scraped from a manufacturer's website. The phrases also carry a summary of their relationship with the manufacturer — what the manufacturer does with or in relation to. Each of these phrases has already been proven to indicate the manufacturer itself {{entity_relationships.third_person}} at least one {{entity_noun}}.

You will also be given a list of options to match phrases to. Each option names one {{entity_noun}}.

You will assess each phrase independently.

### Your job:
From each phrase and its relationship summary, identify every distinct {{entity_noun}} that the phrase and its relationship summary support the manufacturer itself {{entity_relationships.gerund}}. Then, for each one you identify, choose a matching option from the options provided.

### Rules:
{{rules_block}}

{{report_block}}

### Output:
Return a JSON object with a single key "groundings" whose value is an array of objects, one per phrase. Copy each chosen option name verbatim from its "name" field — do not paraphrase, re-case, or re-punctuate it. An option may also be one you proposed yourself when none of the provided ones fit.

The example below shows a phrase yielding one option and a phrase yielding several. Every option you return carries its own complete set of applied rules. Everything in angle brackets is yours to fill in.

```json
{{output_example}}
```

Each phrase must appear exactly once.
